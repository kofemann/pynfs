gssapi
======

Clean up code in general.
Enable use of logging.
Fix exception generation.
What to do about Name() - do we need this class?
Decide on final return result from wrap/unwrap.
Add missing functions. 

rpc
===


xdr
===

server
======
Mounted_on_fileid still causes some problems:
  S.mount(A, path="/a")
  S.mount(B, path="/a/gen/b")
  cd a
  mkdir d1, d2, ...

  Problem occurs when d?.id == gen.id

Speed up readdir.
Speed up write.

Fix clientid generation
Fix stateids/principals to allow binding